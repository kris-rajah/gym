.DS_Store
__pycache__/
*.pyc
*.pyo
.venv/
.env

# Personal plan data — kept local, not published to the public repo.
# Only the rendered docs/*.html (with scrubbed reasons) get pushed.
plan/*.yaml
plan/logs/
plan/rendered/
